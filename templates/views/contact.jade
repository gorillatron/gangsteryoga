extends ../layouts/default

block intro
	.container-fluid
		h1 Kontakt oss
		
block content
	.container-fluid
		if enquirySubmitted
			h3 Takk for meldingen din! Vi svarer deg så fort vi kan :)
		else
			.row: .col-sm-8.col-md-6
				form(method='post')
					input(type='hidden', name='action', value='contact')
					.form-group(class=validationErrors.name ? 'has-error' : null)
						label Navn
						input(type='text', name='name.full', value=formData['name.full']).form-control
					.form-group(class=validationErrors.email ? 'has-error' : null)
						label Epost
						input(type='email', name='email', value=formData.email).form-control
					.form-group
						label Telefon
						input(type='text', name='phone', value=formData.phone, placeholder='(valgfritt)').form-control
					//- .form-group(class=validationErrors.enquiryType ? 'has-error' : null)
					//- 	label Hva gjelder henvendelsen?
					//- 	select(name='enquiryType').form-control
					//- 		option(value='') (select one)
					//- 		each type in enquiryTypes
					//- 			option(value=type.value, selected=formData.enquiryType == type.value)= type.label
					.form-group(class=validationErrors.message ? 'has-error' : null)
						label Melding
						textarea(name='message', placeholder='Legg igjen en beskjed...' rows=4).form-control= formData.message
					.form-actions
						button(type='submit').btn.btn-lg.btn-default Send
